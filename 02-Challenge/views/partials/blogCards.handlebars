<div class="col-md-6 col-lg-4 mb-4">
    <div class="home-item card overflow-hidden">
        <a href="/events/{{id}}" class="text-decoration-none">

            <div class="card-body">
                <h4 class="card-title">{{title}}</h4>
            </div>
            <ul class="list-group list-group-flush">
                <li class="list-group-item">Created by: <span class="h5">{{capitalize user.username}}</span>
            </ul>
        </a>
    </div>
</div>